import React from 'react'

import CreateClient from '../components/CreateClient'

function addClient() {
    return (
        <div>
            <CreateClient />
        </div>
    )
}

export default addClient